<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Maui.Controls.Sample"
             x:Class="Maui.Controls.Sample.ProgressBarControlMainPage"
             Title="ProgressBarFeature">

  <ContentPage.ToolbarItems>
    <ToolbarItem Text="Options" Clicked="NavigateToOptionsPage_Clicked" AutomationId="Options"/>
  </ContentPage.ToolbarItems>

  <Grid Padding="20" RowDefinitions="Auto, Auto, Auto, Auto, Auto, Auto" ColumnDefinitions="0.5*, 0.5*" RowSpacing="10">
    <!-- ProgressBar Control -->
    <ProgressBar Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"  
               Progress="{Binding Progress}"
            ProgressColor="{Binding ProgressColor}"
            BackgroundColor="{Binding BackgroundColor}"
            IsVisible="{Binding IsVisible}"
            IsEnabled="{Binding IsEnabled}"
            FlowDirection="{Binding FlowDirection}"
            Margin="0,100,0,100" AutomationId="ProgressBarControl" />

    <!-- Progress Value -->
    <Label Grid.Row="1" Grid.Column="0" 
               Text="Progress:" 
               FontSize="16" 
               VerticalTextAlignment="Start" 
               HorizontalTextAlignment="Start" 
               Margin="80,0,10,10" />
    <Label Grid.Row="1" Grid.Column="1" 
               Text="{Binding Progress, StringFormat='{0:F2}'}" 
               FontSize="16" 
               VerticalTextAlignment="Start" 
               HorizontalTextAlignment="Start" AutomationId="ProgressValueLabel"/>

    <!-- Progress Color -->
    <Label Grid.Row="2" Grid.Column="0" 
               Text="Progress Color:" 
               FontSize="16" 
               VerticalTextAlignment="Start" 
               HorizontalTextAlignment="Start" 
               Margin="80,0,10,10" />
    <Grid Grid.Row="2" Grid.Column="1">
      <Rectangle Fill="{Binding ProgressColor}" WidthRequest="20" HeightRequest="20" AutomationId="ProgressColorIndicator"/>
    </Grid>

    <!-- Button to change progress -->
    <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
           Text="Change Progress"
           Command="{Binding ChangeProgressCommand}"
           AutomationId="ChangeProgressButton"/>
  </Grid>
</ContentPage>
